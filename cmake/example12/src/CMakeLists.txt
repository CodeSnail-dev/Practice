include_directories(${PROJECT_SOURCE_DIR}/libhello)
set(APP_SRC main.cpp)
set(EXECUTABLE_OUTPUT_PATH ${PROJECT_BINARY_DIR}/bin)
add_executable(NaviHMI ${APP_SRC})
target_link_libraries(NaviHMI libNavShell)